---
title: "DelowlightSplat: Feed-Forward Gaussian Splatting for Lowlight 3D Scene Reconstruction"
title_zh: DelowlightSplat：用于低光三维场景重建的前馈高斯泼溅
authors: "Fuzhen Jiang, Zengtian Xie, Zhuoran Li"
date: 2026-05-26
pdf: "https://arxiv.org/pdf/2605.26629v1"
tags: ["query:gs-slam"]
score: 9.0
evidence: 使用前馈3D高斯泼溅从稀疏姿态图像进行新视角合成
tldr: 从稀疏图像进行视角合成与三维重建对机器人和AR/VR至关重要，但前馈式3D高斯溅射方法在低光环境下因噪声、色偏和不可靠匹配而失效。DelowlightSplat提出一种低光感知的前馈框架，通过构建可控多视角低光基准，设计轻量低光适配器进行残差增强提升匹配性，结合代价体多视图推理直接预测干净高斯。实验表明该方法显著超越以往前馈方法和两阶段流程，为暗光三维重建提供了有效的前馈方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.26629v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1499, \"height\": 483, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.26629v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1506, \"height\": 793, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.26629v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 830, \"height\": 606, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.26629v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 745, \"height\": 261, \"label\": \"Table\"}]"
motivation: 前馈高斯重建在低光下因噪声、色移和匹配不可靠而失败，限制了暗环境3D场景理解。
method: 构建可控多视图低光基准，引入轻量Lowlight Adapter进行残差增强来改善匹配性，并用代价体多视图推理直接预测干净3D高斯。
result: 在低光条件下，DelowlightSplat显著优于先前的前馈方法与两阶段管线，实现了高清新视角渲染。
conclusion: DelowlightSplat为低光三维重建提供了有效前馈方案，缓解了匹配难题，推动了暗场景重建与理解应用。
---

## 摘要
从稀疏位姿图像进行新视角合成和三维重建是机器人与AR/VR的核心任务。然而，前馈三维高斯重建在低光条件下因噪声、色彩偏移和不可靠的对应关系而失败。我们提出DelowlightSplat，一种面向清晰新视角渲染的低光感知前馈高斯泼溅框架。通过仅退化上下文视图而保持目标视图清晰，我们构建了一个可控的多视图低光基准。我们引入轻量级低光适配器进行残差增强以提升可匹配性，并将其与基于成本体的多视图推理相结合，直接预测清晰的三维高斯。实验表明，DelowlightSplat在低光条件下显著优于先前的前馈方法和两阶段流程。

## Abstract
Novel-view synthesis and 3D reconstruction from sparse posed images are central to robotics and AR/VR. Yet, feed-forward 3D Gaussian reconstruction fails under lowlight due to noise, color shifts, and unreliable correspondence. We propose DelowlightSplat, a lowlight-aware feed-forward Gaussian splatting framework for clean novel-view rendering. We build a controllable multi-view lowlight benchmark by degrading only context views while keeping target views clean. We introduce a lightweight Lowlight Adapter for residual enhancement to improve matchability, and couple it with cost-volume-based multi-view inference to directly predict clean 3D Gaussians. Experiments show that DelowlightSplat significantly outperforms previous feed-forward method and two-stage pipeline under lowlight conditions.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **研究背景**：从稀疏位姿图像进行新视角合成与三维重建是机器人、增强现实/虚拟现实等应用的基础。3D Gaussian Splatting（3DGS）及前馈式预测方法在常规曝光下效果优异，但在低光环境下面临噪声严重、色彩偏移和跨视图几何匹配失效等问题，导致重建质量骤降。
- **核心问题**：现有前馈三维高斯重建方法无法在低光输入下可靠工作，而简单的“先增强后重建”两阶段方案会引入视图间不一致，破坏多视图几何聚合。因此，需要一种能够直接在低光输入上稳定预测干净三维高斯场景的前馈框架。
- **整体含义**：DelowlightSplat旨在实现端到端的低光感知前馈重建，通过轻量级低光适配与多视图代价体推理，直接从退化图像恢复清晰、光照正常的新视角渲染结果，为暗光场景理解与合成提供有效方案。

### 2. 论文提出的方法论

- **核心思想**：不依赖外部图像增强模型，而是在前馈重构网络前端嵌入一个可学习的低光适配器，以残差方式修正输入，改善跨视图匹配性；随后由多视图代价体模块推理几何与外观，直接预测符合正常光照的三维高斯表示。
- **关键技术细节**：
    - **可控多视图低光基准构建**：仅对输入上下文视图施加一系列概率性退化（Gamma暗化、曝光缩放、RGB通道偏移、高斯模糊），目标视图保持原始干净图像。该协议使模型学会从退化观测中恢复干净场景，而非简单模仿低光外观。
    - **轻量级低光适配器**：给定低光视图 \(I_i\)，通过卷积残差块预测残差图 \(\Delta(I_i)\)，再与原始输入融合并裁剪范围：\(\tilde{I}_i = \text{clip}(I_i + \lambda \cdot \tanh(\Delta(I_i)))\)。该模块端到端可训练，起到提升特征可匹配性而非单独美化的作用。
    - **前馈高斯泼溅网络**：对适配后的多视图提取2D特征，经跨视图代价体交互获得几何一致的深度与外观信息；由深度反投影生成三维高斯中心，结合预测的尺度、旋转和透明度构建高斯集合；每个高斯携带球谐系数以表示视角相关颜色。训练时以干净目标视图为监督，混合像素损失与感知损失优化。
- **公式要点**：
    - 高斯中心：\(\mu(u,d) = o + d \cdot r(u;K,T)\)。
    - 训练损失：\(L = L_{\text{pix}}(\hat{I}_t, I_t^*) + \lambda_{\text{per}} L_{\text{per}}(\hat{I}_t, I_t^*)\)。

### 3. 实验设计

- **数据集与基准**：基于大规模位姿数据集（引用[11]，即RealEstate10K相关或类似数据集）构建可控低光基准，只退化上下文视图，保持目标视图不变。
- **对比方法**：
    - **Lowlight MVSplat**：直接将低光视图输入经典前馈重构器MVSplat，测试低光对原模型的破坏程度。
    - **DarkIR → MVSplat**：先用先进的2D低光恢复模型DarkIR逐视图增强，再将增强结果送入MVSplat重构，代表强“恢复-重建”两阶段基线。
    - **DelowlightSplat（本方法）**：集成低光适配器与多视图推理的端到端方案。
- **评估指标**：PSNR、SSIM、LPIPS，用于衡量新视角合成质量。

### 4. 资源与算力

- 文中明确说明：使用一块NVIDIA RTX 4090 GPU，训练批次大小为8，总训练步数10,000步。无多卡分布或训练时长的进一步描述。

### 5. 实验数量与充分性

- **主实验**：在低光基准上对比三种方法，给出定量表格（表I）与定性可视化（图2）。实验设定清晰，对比公平，涵盖直接重构与典型两阶段方案。
- **消融实验**：逐步添加单项退化（Gamma暗化→曝光缩放→RGB偏移→模糊），评估模型在各退化水平下的表现，并展示各阶段退化图像与适配结果（图3、表II）。消融设计系统地分析了不同低光因素对重建的影响及适配器的缓解效果。
- **实验充分性评价**：实验覆盖了主要的低光退化类型，对比基线具备代表性，消融实验逻辑清晰。但受限于基准为合成退化，缺少真实传感器低光数据（如Raw噪声、动态范围压缩）的验证；仅使用两视图输入，未扩展到更多视角或时序场景，实验多样性略有不足。

### 6. 论文的主要结论与发现

- 直接应用常规前馈高斯重建在低光条件下严重失效，PSNR仅8.49 dB，产生过度平滑纹理和几何漂移。
- 两阶段“先增强后重建”基线虽提升亮度，但因视图间不一致导致模糊和色差，重建质量仍有限。
- DelowlightSplat通过嵌入低光适配器显著增强跨视图可匹配性，从而稳定代价体推理，在PSNR上较Lowlight MVSplat提升14.59 dB（达到23.08 dB），较DarkIR→MVSplat提升5.97 dB，SSIM与感知指标亦有大幅改善。
- 适配器的残差设计使其在退化轻微时接近恒等映射，避免过度增强；在不同单因素退化下均表现稳健。

### 7. 优点

- **问题定位精准**：针对低光下前馈重建特有的匹配失败问题，提出端到端的改善思路。
- **方法简洁高效**：仅在前端添加小型残差适配器，无需大幅修改基础重构网络，且适配器为轻量级。
- **基准构建合理**：通过可控退化独立上下文视图，避免了真实低光配对数据的匮乏，且隔离了低光对重建的影响。
- **性能提升显著**：对比直接重建和两阶段方案均有大幅指标提升，且定性结果细节更锐利、颜色更一致。
- **实验分析扎实**：消融实验逐步分析各退化因素的影响，验证了适配器设计的有效性。

### 8. 不足与局限

- **真实场景缺失**：所有实验均基于合成退化，未在真实低光传感器数据（如Raw域噪声、泊松-高斯噪声、动态范围问题）上评估，实际应用中性能可能打折。
- **输入规模受限**：仅支持两视图输入，尚未拓展到更多视角或视频序列，而实际应用中往往可获得更多观测。
- **适配器局限性**：轻量残差设计对极端噪声、严重运动模糊或空间变化退化的鲁棒性可能有限，且未探索更先进的恢复模块或Raw域处理。
- **未讨论计算开销**：虽称适配器“轻量”，但未给出具体参数量、推理时间或对比基线的延迟差异，部署效率不透明。
- **对比方法有限**：未与其他低光新视角合成方法（如基于NeRF的暗光方法）或多种增强策略进行比较，方法涵盖面偏窄。

（完）
