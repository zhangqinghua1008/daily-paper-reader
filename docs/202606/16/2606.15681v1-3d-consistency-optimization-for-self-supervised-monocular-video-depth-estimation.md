---
title: 3D Consistency Optimization for Self-Supervised Monocular Video Depth Estimation
title_zh: 自监督单目视频深度估计的三维一致性优化
authors: "Yuanye Liu, Ke Zhang, Junzhe Jiang, Li Zhang, Vishal Patel, Xiahai Zhuang"
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.15681v1"
tags: ["query:endo-dgs"]
score: 9.0
evidence: 自监督单目视频深度估计用于内窥镜导航
tldr: 现有自监督单目视频深度估计方法独立处理帧，缺乏全局3D感知，导致几何不一致和帧间漂移。本文将视频深度估计重新定义为无约束多视图3D重建问题，提出3D一致性优化框架，联合光度渲染、世界坐标几何对齐和多尺度时序梯度一致性约束。实验在自监督训练和零样本临床环境均达到最先进空间准确性，显著优于帧基、视频基深度估计及多视图重建基线。该方法为内窥镜导航等下游3D推理提供更可靠的深度估计。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15681v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1222, \"height\": 485, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15681v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1265, \"height\": 435, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15681v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1244, \"height\": 1004, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15681v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1244, \"height\": 1004, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15681v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1245, \"height\": 335, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15681v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1253, \"height\": 309, \"label\": \"Table\"}]"
motivation: 现有自监督方法忽略全局3D结构，导致预测几何不一致和严重跨帧漂移。
method: 将视频深度估计转化为多视图3D重建，提出3D一致性优化框架，结合光度渲染、显式几何对齐和时序梯度一致性三个约束。
result: 在自监督和零样本临床环境中均获得最先进的空间准确性，超越帧基、视频基方法和多视图重建基线。
conclusion: 通过全局3D一致性约束，显著提升单目视频深度估计的可靠性与下游任务适用性。
---

## 摘要
可靠的单目视频深度估计对内窥镜导航中的下游三维推理和具身人工智能至关重要。然而，现有的自监督方法通常独立处理视频帧或依赖于弱时间正则化。这些方法缺乏对底层三维场景的整体感知，不可避免地会出现几何不一致的预测和严重的跨帧漂移。为了解决这些局限性，我们引入了一种新范式，将序列视频深度估计重新定义为无约束的多视图三维重建问题，从而能够充分利用最近三维基础模型中嵌入的强大几何先验。我们方法的核心是一个由三个约束驱动的三维一致性优化框架：图像级别的光度渲染、显式的世界坐标几何对齐以及多尺度时间梯度一致性。这种统一的优化优雅地将孤立的帧锚定到全局连贯的三维结构中。我们的方法已在自监督训练场景和具有挑战性的零样本临床环境中得到验证。结果表明，所提出的方法实现了最先进的空间精度，优于基于帧的、基于视频的深度估计器和多视图三维重建基线。

## Abstract
Reliable monocular video depth estimation is crucial for downstream 3D reasoning and embodied AI in endoscopic navigation. However, existing self-supervised approaches typically treat video frames independently or rely on weak temporal regularization. These methods, lacking a holistic perception of the underlying 3D scene, inevitably suffer from geometrically inconsistent predictions and severe cross-frame drift. To address these limitations, we introduce a new paradigm that recasts sequential video depth estimation as an unconstrained multi-view 3D reconstruction problem, enabling full exploitation of the powerful geometric priors embedded in recent 3D foundation models. The core of our approach is a 3D consistency optimization framework driven by three constraints: image-level photometric rendering, explicit world-coordinate geometric alignment, and multi-scale temporal gradient consistency. Such unified optimization elegantly anchors isolated frames to a globally coherent 3D structure. Our method has been validated in both the self-supervised training scenarios and challenging zero-shot clinical environments. Results show that the proposed approach achieves state-of-the-art spatial accuracy, outperforming the frame-based, video-based depth estimators and the multi-view 3D reconstruction baselines.

---

## 论文详细总结（自动生成）

# 论文总结：《3D Consistency Optimization for Self-Supervised Monocular Video Depth Estimation》

## 1. 论文的核心问题与整体含义
- **核心问题**：现有的自监督单目内窥镜视频深度估计方法通常将视频帧视为独立图像处理，或仅施加弱时间正则化，缺乏对底层三维场景的整体感知。这会导致预测的深度图在几何上不一致，并产生严重的跨帧空间漂移和时间闪烁，严重削弱下游三维重建和导航的可靠性。
- **整体含义**：本文提出一种范式转变，将序列视频深度估计重新定义为无约束的多视图三维重建问题，使单目内窥镜视频能够利用现代三维基础模型（如 `AnySplat`）的强大几何先验。通过构建三维一致性优化框架，将孤立帧锚定到全局一致的三维结构中，从而在无位姿标注的条件下实现几何连贯、时间稳定的深度估计。

## 2. 方法论
- **核心思想**：将内窥镜视频看作对同一三维场景的多视图观测，用可微分三维高斯泼溅（3D Gaussian Splatting）表示场景，联合优化深度图、相机位姿和内参。训练时施加图像、世界坐标和时间维度上的一致性约束，使模型学会在单次前馈推理中预测出时空一致的深度。
- **关键技术细节**：
    - **场景表示**：使用一组三维高斯基元 `P = {p_i}` 参数化场景，包含中心、不透明度、协方差、球谐系数等属性。通过可微分渲染器 `R(P, T_t, K)` 同时输出预测的 RGB 图像 `Î_t` 和深度图 `D̂_t`。
    - **三维几何理解网络**：基于多视图 Transformer 设计，输入由 DINOv2 特征标记，交替执行帧内注意力和全局帧间注意力。输出三个头：相机头（预测位姿 `T_t` 和内参 `K`）、深度头（预测初始深度 `D_init_t` 用于反投影初始化高斯中心）、高老头（预测其他高斯属性）。
    - **三维一致性优化框架**（仅训练时使用，推理为单次前馈）：
        - **图像一致性损失 `L_rgb`**：最小化渲染图像与原始帧的 L1 误差，迫使三维结构在图像空间逼真。
        - **世界坐标几何一致性损失 `L_3D`**：将帧 `t` 的像素 `u` 通过深度 `D̂_t(u)` 反投影到世界点 `X_t(u)`，再投影到相邻帧 `t'`，比较 `t'` 的预测深度与重投影深度的 Huber 误差。只在有效像素集 `Ω_{t,t'}`（内、正深度）内计算，有效抑制空间漂移。
        - **时间一致性损失 `L_temp`**：在多尺度下，计算相邻帧深度图的空间梯度差异（L1 损失），并用有效掩膜 `Ω_t` 过滤，平滑时间闪烁，同时保留几何边缘。
    - **总损失**：`L = λ1 L_rgb + λ2 L_3D + λ3 L_temp`，权重均设为 1。

## 3. 实验设计
- **数据集**：
    - **SCARED**：da Vinci Xi 手术系统采集的 35 段高分辨率内窥镜视频（22,950 帧），提供投影仪测量的真实深度、相机位姿和内参。按照 EndoDAV [22] 的划分：24 段训练，3 段验证，8 段测试，用于评估空间精度和时间一致性。
    - **Hamlyn**：包含多种体内内窥镜和腹腔镜手术视频，具有动态组织运动和复杂相机轨迹，使用经过 [15] 校正的版本，以 Libelas 生成的真实深度图，全部用于零样本泛化评估。
- **Benchmark**：对比方法包括帧级别的 `EndoDAC` (2024)、视频级别的 `VDA` (2025) 和 `EndoDAV` (2025)，以及多视图三维重建基线 `AnySplat` (2025)。采用相同的数据划分和评估设置。
- **评估指标**：Abs Rel ↓, Sq Rel ↓, RMSE ↓, RMSE log ↓, δ1 ↑。预测深度通过中值缩放与真实值对齐，以公平评估仿射不变深度。

## 4. 资源与算力
- 论文明确说明：使用 PyTorch 实现，在**单块 NVIDIA A100 GPU** 上训练。优化器为 AdamW，共 **20000 次迭代**，包含 2000 次 warmup。每次迭代采样一段 16 帧的内窥镜视频片段。峰值学习率 2×10⁻⁴，某些参数使用 0.1 倍学习率乘子。24 层交替注意力 Transformer，权重从 AnySplat 初始化。训练过程中相机头在 warmup 阶段冻结后解冻，其余预训练组件保持冻结。

## 5. 实验数量与充分性
- **主实验**：在 SCARED 和 Hamlyn 两个数据集上进行定量对比，涵盖 4 个代表性 baseline，结果以表格呈现（Table 1）。
- **消融实验**：
    - 对三种一致性损失进行组件消融（Table 2），从原始 AnySplat 开始逐步添加 `L_rgb`、`L_3D`、`L_temp`，证明各项的互补作用。
    - 跨骨架泛化实验（Table 3），将一致性优化应用于 VDA 和 AnySplat 两个不同骨架，验证其普适性。
- **定性对比**（Fig. 3）：展示小运动和大运动场景下的深度图可视化，突出结构保持和误差抑制。
- 实验设计全面，对比公平（相同数据划分、指标对齐策略），消融充分验证了各组件的有效性，跨骨架实验增强了说服力。

## 6. 主要结论与发现
- 所提出的三维一致性优化框架在 SCARED 和 Hamlyn 上均显著优于现有最佳方法，例如在 SCARED 上将 Abs Rel 从 0.100（AnySplat）降至 0.060，RMSE 从 10.400 降至 5.503；在 Hamlyn 零样本设置下 Abs Rel 从 0.299 降至 0.108。
- 消融实验表明，图像光度损失、世界坐标几何对齐损失和时间梯度一致性损失相互补充，共同贡献了最高的几何精度和时间稳定性。
- 该一致性优化模块可即插即用地提升不同深度估计骨架的性能（如 VDA 的 Abs Rel 从 0.241 降至 0.097）。
- 范式转变（从单帧估计到多视图三维重建）是解决内窥镜视频深度不一致和漂移的有效途径。

## 7. 优点
- **范式创新**：首次将自监督单目视频深度估计与多视图三维重建统一，利用三维基础模型先验，摆脱了传统弱时序约束的局限。
- **约束设计巧妙**：三项损失分别在图像空间、三维世界空间和时间梯度空间施加一致性，互补性强，能有效消除空间漂移和时间闪烁，同时保留几何边缘。
- **高效推理**：一致性优化仅在训练时使用，推理为单次前馈，无测试时优化的开销，适合实时临床应用。
- **泛化性强**：在零样本的复杂临床环境（Hamlyn）中表现优异，且能泛化到不同骨架（VDA），证明方法的鲁棒性和通用性。
- **实验严谨**：多个数据集、公平对比、详尽的消融和可视化，说服力强。

## 8. 不足与局限
- **场景局限性**：论文主要针对内窥镜手术场景，未在其他类型视频（如自动驾驶、通用视频）上验证，泛化边界未明确。
- **动态组织影响**：虽然使用了 Huber 损失和有效掩膜处理动态运动，但未专门建模非刚性形变，复杂组织变形下可能仍存在误差。
- **依赖初始化**：网络权重从 AnySplat 初始化，对预训练模型有一定的依赖，从零训练的有效性未探讨。
- **计算需求**：训练在 A100 上进行 20000 次迭代，每次 16 帧，训练代价对于部分研究组可能较高，但未与 baseline 对比训练开销。
- **评估公平性细节**：Hamlyn 的真实深度由 Libelas 生成，可能引入系统偏差，且仅进行尺度对齐，绝对度量可能受尺度模糊影响，但文中已公认处理方式。

（完）
