---
title: Geometry-Consistent Endoscopic Representations for Image-Guided Navigation via Structured Foundation Model Adaptation
title_zh: 基于结构化基础模型自适应的几何一致性内镜表示用于图像引导导航
authors: "Hongchao Shu, Roger D. Soberanis-Mukul, Hao Ding, Morgan Ringel, Mali Shen, Saif Iftekar Sayed, Hedyeh Rafii-Tari, Mathias Unberath"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.17340v1"
tags: ["query:endo-dgs"]
score: 9.0
evidence: 统一表征学习用于内窥镜导航中的几何一致性深度与姿态估计。
tldr: 单目内窥镜导航面临深度缺失、纹理弱、变形大等挑战，现有基础模型表示几何一致性差。本文提出合成数据与层次感知几何语义适配的统一框架，选择性插入低秩适配器并耦合逐层训练，强化中间几何对应与深层语义一致性。在支气管镜、鼻窦镜和结肠镜等实验中，该方法显著提升姿态估计与深度估计，展现合成到真实泛化与模型扩展性，为内窥镜表示学习提供实用方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17340v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1843, \"height\": 407, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17340v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 912, \"height\": 364, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17340v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1851, \"height\": 505, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17340v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 900, \"height\": 1130, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17340v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 904, \"height\": 1014, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.17340v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1839, \"height\": 478, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17340v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 902, \"height\": 339, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17340v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 908, \"height\": 195, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17340v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 911, \"height\": 426, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17340v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 915, \"height\": 375, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17340v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 904, \"height\": 299, \"label\": \"Table\"}]"
motivation: 现有视觉基础模型所学表示在内窥镜中几何一致性不足，难以支撑稳定特征对应与可靠导航。
method: 结合合成数据几何监督与层次感知几何语义适配，在Transformer中分层插入低秩适配器并耦合逐层训练目标。
result: 在多种内窥镜数据上显著提升姿态估计和深度估计精度，展示合成到真实泛化及跨域适应能力。
conclusion: 层次感知几何引导适配为内窥镜表示学习提供了有效且可扩展的框架，具有实用价值。
---

## 摘要
单目内镜中基于视觉的精确导航因深度线索有限、组织纹理微弱、非刚性变形及跨域显著外观变化而困难重重，这些因素使姿态估计、深度预测和图像到解剖结构的对齐更加复杂。尽管近期视觉基础模型展现出潜力，但其学习到的表示往往几何一致性不足，阻碍稳定的特征对应，限制了下游导航任务的可靠性。我们提出一个统一框架，用于学习单目内镜的几何一致且域鲁棒的图像表示。该框架结合了提供精确几何监督的合成数据流水线与层级感知的几何-语义自适应——这是对标准LoRA的结构化替代方案，它在变换器层级中有选择地插入低秩适配器，并辅以分层训练目标，以促进中间特征的几何对应和深层特征的语义一致性。在公开和专有数据集上的实验表明，几何和语义表示质量得到提升，从而在下游导航任务（包括姿态估计和单目深度估计）中取得更好性能。学习到的表示在临床支气管镜检查中展现出良好的合成到真实迁移能力，并为在有限监督下适应鼻窦内镜和结肠镜检查提供了有用的初始化。该框架还显示出对模型规模和数据量的良好扩展性。这些结果支持层级感知、几何引导的自适应作为内镜表示学习的实用方法。

## Abstract
Accurate vision-based navigation in monocular endoscopy is difficult due to limited depth cues, weak tissue texture, non-rigid deformation, and substantial appearance variation across domains, all of which complicate pose estimation, depth prediction, and image-to-anatomy alignment. Although recent vision foundation models have shown promise, their learned representations often remain insufficiently geometry-consistent, hindering stable feature correspondence and limiting their reliability for downstream navigation tasks. We propose a unified framework for learning geometry-consistent and domain-robust image representations for monocular endoscopy. The framework combines a synthetic data pipeline that provides accurate geometric supervision with Hierarchy-Aware Geometry-Semantic Adaptation, a structured alternative to standard LoRA that inserts low-rank adapters selectively across the transformer hierarchy and couples them with layer-wise training objectives to encourage geometric correspondence in intermediate features and semantic consistency in deeper features. Experiments on public and proprietary datasets show improved geometric and semantic representation quality, leading to better performance on downstream navigation tasks including pose estimation and monocular depth estimation. The learned representations show favorable synthetic-to-real transfer on clinical bronchoscopy and provide a useful initialization for adaptation to sinus endoscopy and colonoscopy under limited supervision. The framework also shows favorable scaling with model size and training data. These results support hierarchy-aware, geometry-guided adaptation as a practical approach for endoscopic representation learning.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **核心问题**：单目内窥镜图像引导导航中，视觉基础模型（如 DINOv2）虽语义能力强大，但其学习到的图像表示 **几何一致性不足**，导致特征对应不稳定，限制了其在姿态估计、深度预测等空间推理任务中的可靠性。
- **研究动机**：
    - 内窥镜图像存在 **深度线索缺失、纹理弱、非刚性形变、高光伪影** 及 **跨域（机构、设备、术式）外观差异显著** 等固有挑战。
    - 真实临床环境中 **难以获取密集的几何标注**（如精确深度、6-DoF 姿态），阻碍了强几何监督方法的大规模部署。
    - 现有基础模型适配工作多聚焦于语义感知， **缺乏对几何一致性的显式约束**。
- **整体含义**：作者旨在建立一个统一框架，通过 **结构化适配** 与 **合成数据几何监督**，赋予通用视觉基础模型 **几何一致性** 与 **域鲁棒性**，从而为下游导航任务提供更稳定、可迁移的视觉表示。

### 2. 论文提出的方法论

- **核心思想**：利用 **合成数据精确几何信号**，结合 **变换器架构层级特性**，通过在基础模型（DINOv2 ViT）的不同层有选择地插入低秩适配器（LoRA）并施加分层训练目标，引导模型在 **中间层学习几何对应**，在 **深层保持语义一致性**。
- **关键技术框架**：
    - **多域合成数据流水线**：
        - 从 CT 重建的 3D 解剖模型渲染配对的内窥镜视图（含深度图与相对姿态）。
        - 通过 **域随机化**（CycleGAN 风格迁移、光度扰动、模态混叠）增大视觉多样性，同时保持几何监督的准确性。
    - **层级感知几何-语义自适应（HGSA）**：
        - 打破标准 LoRA 的均匀插入模式，转为对变换器分层 **粗到细的渐进式搜索**，确定适配器的最优插入层位、投影矩阵、秩与缩放因子。
        - 搜索依据：使用 **线性探测** 在语义分类与深度估计两个互补任务上的综合评分。
    - **几何与语义感知监督**：
        - **几何感知特征对应**：利用已知渲染几何与相机位姿，计算视图间的稠密流场，对中间层特征进行 **可微扭曲**，并施加多尺度、基于掩膜的 **密集对比损失** 与 **余弦重投影损失**。
        - **语义感知一致性**：对深层全局令牌施加 **实例级对比损失**，使同场景不同视角的表征聚拢，无关场景推远。
        - **结构保持正则化**：引入基于 **冻结 DINO 教师模型** 的 Gram 矩阵蒸馏损失，防止强语义约束破坏中间特征的空间结构。
- **公式与流程再现**：
    - **适配器设计空间**：$\mathcal{C} = \{(\mathcal{L}_a, r, \alpha, \mathcal{M})\}$，包含插入层 $\mathcal{L}_a$、秩 $r$、缩放因子 $\alpha$、目标投影矩阵 $\mathcal{M}$。
    - **特征扭曲**：$\tilde{z}_b = \mathcal{W}(z_b, F_{b\to a})$，其中 $F_{b\to a}$ 为已知的几何流场。
    - **总训练目标**：$\mathcal{L} = \lambda_{ms} \mathcal{L}_{ms} + \lambda_{nce} \mathcal{L}_{\text{InfoNCE}} + \lambda_{gram} \mathcal{L}_{gram}$，由几何密集损失、语义对比损失和结构蒸馏损失三部分平衡组成。

### 3. 实验设计

- **数据集**：
    - **训练**：仅使用 **合成数据**，包括支气管镜、鼻窦镜和结肠镜的渲染对（主实验用支气管镜 **481,650** 对）。
    - **评测**：**真实临床视频**，包括支气管镜（20,555 帧，无直接6-DoF真值，使用对齐代理指标）、鼻窦镜（1,107 帧，有真值）、结肠镜（C3VD 数据集，1,176 帧，有真值）。
- **任务基准与对比方法**：
    - **表示质量探测**：在冻结特征上进行线性分类（场景分类）和深度估计，对比不同 HGSA 配置及消融变体。
    - **下游导航任务评测**：
        - **姿态估计**：采用统一的 BronchOpt 优化框架，对比 **DINOv3（原生）**、**DINOv3（全层 LoRA）** 和本文方法。
        - **单目深度估计**：采用统一的 DPT 解码器，对比 **Depth Anything v2**（直接推理）、**DINOv3（原生）**、**DINOv3（全层 LoRA）** 和本文方法。
    - **跨域迁移**：将支气管镜预训练的模型，在小样本鼻窦/结肠合成数据上微调后评测。
    - **可扩展性**：对比 **ViT-S/B/L** 不同容量骨干，及使用 **25%/50%/100%** 数据量训练。

### 4. 资源与算力

- 论文正文与补充材料中 **未明确提及** 具体的 GPU 型号、数量或总训练时长。
- 文中仅提及模型为 ViT 架构（S/B/L），但未给出对算力消耗的详细描述。

### 5. 实验数量与充分性

- **大量消融与配置搜索实验**：
    - **HGSA 渐进式搜索**：设计了三阶段（层位→模块→秩/缩放）的结构化搜索，共评估 **41 种**不同的配置组合。
    - **组件消融实验**：分解并验证了 HGSA、扭曲监督、几何密集损失、语义对比损失、结构蒸馏损失等核心组件的贡献。
- **多任务、多域评测**：
    - 覆盖了 **支气管镜、鼻窦镜、结肠镜** 三个差异显著的术式。
    - 同时评测了 **表示质量（语义+几何）、姿态估计、深度估计**，共三类不同粒度的任务。
- **实验充分性与公平性**：实验设计客观且较为充分。所有下游任务对比均在统一的解码器/优化框架下进行，仅更换冻结的编码器，公平地隔离了表示质量的影响。多维度消融和跨域评估增强了结论的鲁棒性。

### 6. 论文的主要结论与发现

- **HGSA 是一种有效的适配策略**：相比全层或无适配器，层级感知的结构化插入能更好地平衡几何与语义表示质量。
- **表示质量显著提升**：学习到的特征既实现了 **跨域（真实/合成RGB/深度）对齐**，又保留了更强的 **空间对应一致性**。
- **下游导航任务性能稳定提升**：
    - **姿态估计**：在支气管镜、结肠镜、鼻窦镜上，图像-渲染对齐指标（NC、DS、SI）及姿态误差均有一致或显著改善。
    - **深度估计**：在结肠和鼻窦数据上，AbsRel、LogRMSE、$\delta_1$ 等指标全面超越同等训练协议下的原生 DINOv3 及全层 LoRA 方法，并在支气管镜上展示出更符合解剖结构的预测。
- **良好的泛化与扩展性**：
    - 证明了从合成支气管镜数据向真实临床视频的 **有效 sim-to-real 迁移**。
    - 展示了对 **骨干模型容量和训练数据量** 的良好可扩展性。

### 7. 优点

- **方法论上的创新整合**：巧妙地将合成数据几何监督、变换器层级特性、参数高效适配与多目标优化统一为一个框架，为解决内窥镜几何表示学习提供了新范式。
- **HCGS 的结构化搜索**：并非简单的应用 LoRA，而是通过严谨的搜索实验，揭示了适配器插入位置、容量与目标模块间的相互作用，为理解 ViT 适配提供了有价值的洞见。
- **评估体系周全**：从**表示质量探测**到 **下游任务实用**，再到 **跨域泛化**，构建了层层递进的验证体系，实验结论说服力强。
- **极强的实用导向**：仅需合成数据训练，即可直接迁移到真实数据，无需任何真实几何标注，大幅降低了临床部署门槛。

### 8. 不足与局限

- **算力成本未公开透明**：缺少关于模型训练所需具体算力的信息，使得方法的资源门槛难以评估。
- **机理分析浅尝辄止**：虽然通过搜索找到了有效的配置，但对“为何某些层位/模块的组合能更好地平衡几何-语义”这一问题，缺乏更深入的机制性解释。
- **鼻窦镜域迁移增益有限**：在鼻窦镜场景下，姿态和深度估计的改善程度不如结肠镜显著，论文将其归咎于数据量不足，但这也暴露了方法在数据稀缺且解剖、纹理变异极大场景下的固有局限。
- **深度估计范围受限**：单目深度估计的绝对精度仍然有限，可能仅适用于相对空间推理，而难以胜任需要绝对测量精度（如测量病灶尺寸）的任务。
- **非实时性**：论文未讨论推理速度，但在复杂的实时手术导航中，特征提取与下游优化的推理延迟是一个关键约束。

（完）
