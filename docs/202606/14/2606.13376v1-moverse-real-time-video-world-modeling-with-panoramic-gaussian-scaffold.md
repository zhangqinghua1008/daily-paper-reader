---
title: "MoVerse: Real-Time Video World Modeling with Panoramic Gaussian Scaffold"
title_zh: MoVerse：基于全景高斯支架的实时视频世界建模
authors: "Yang Zhou, Ziheng Wang, Yuqin Lu, Haofeng Liu, Jun Liang, Shengfeng He, Jing Li"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.13376v1"
tags: ["query:gs-slam"]
score: 8.0
evidence: 构建一个持久的三维高斯支架，从单一窄视场图像实现实时交互式世界建模，支持体积场景表示和实时渲染
tldr: 从单张窄视场图像创建可实时导航的视频世界模型面临观测缺失与实时性挑战。MoVerse通过拓扑感知扩散补全360°全景，并提升为3D高斯脚手架，再结合蒸馏的自回归渲染器实现条件视频生成。在RTX 4090上达到8 FPS实时漫游，验证了单图世界生成与交互的可行性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.13376v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1703, \"height\": 712, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13376v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1622, \"height\": 696, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13376v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1710, \"height\": 822, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13376v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1718, \"height\": 805, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13376v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1716, \"height\": 413, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13376v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1705, \"height\": 898, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13376v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1719, \"height\": 1320, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13376v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1532, \"height\": 2155, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13376v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1722, \"height\": 1318, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13376v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1546, \"height\": 1065, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.13376v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 1547, \"height\": 1031, \"label\": \"Figure\"}]"
motivation: 解决单窄视场图像实时生成可导航全景视频世界的难题，输入观测有限但需完整场景和实时交互。
method: 先拓扑感知扩散生成全景图，然后构建3D高斯脚手架，再用蒸馏的自回归视频渲染器沿相机轨迹生成逼真视频。
result: 在单块RTX 4090上实现8 FPS实时场景漫游，输出高保真且时间连贯的视频。
conclusion: MoVerse结合显式3D表示与生成式视频模型，实现了单图实时视频世界建模，展现了可控性和长程一致性。
---

## 摘要
我们提出MoVerse，一个实时视频世界模型，能够从单张窄视场图像创建交互式可导航场景。该设置具有挑战性，因为输入仅观察到环境的一小部分，而交互式漫游需要完整的周围世界、持久的几何结构、可控的摄像机运动以及时间连贯的高保真观察。MoVerse通过将世界构建与观察渲染分离来解决这个问题。它首先利用拓扑感知扩散将输入扩展为重力对齐的360°全景图，在三维推理之前闭合缺失的视场。然后，利用全景几何感知残差预测将全景图提升为持久的三维高斯支架，产生密集且可直接渲染的空间记忆。最后，高斯条件视频渲染器将沿用户指定摄像机轨迹的支架渲染转换为逼真的视频。为了使该渲染器适用于交互，我们训练了一个双向扩散教师模型以实现高质量条件渲染，并将其蒸馏为因果自回归学生模型以实现有界延迟流式传输。该设计结合了显式三维表示的可控性和长程一致性以及生成式视频模型的感知质量。MoVerse在单个NVIDIA RTX 4090 GPU上以约8 FPS的速度支持实时场景漫游，展示了通向具有交互式视频输出的单图像世界创建的实用路径。

## Abstract
We present MoVerse, a real-time video world model that creates an interactively navigable scene from a single narrow-field-of-view image. This setting is challenging because the input observes only a small fraction of the environment, while interactive roaming requires a complete surrounding world, persistent geometry, controllable camera motion, and temporally coherent high-fidelity observations. MoVerse addresses this problem by separating world construction from observation rendering. It first expands the input into a gravity-aligned 360$^\circ$ panorama with topology-aware diffusion, closing the missing field of view before 3D reasoning. It then lifts the panorama into a persistent 3D Gaussian scaffold using panoramic geometry-aware residual prediction, yielding a dense and directly renderable spatial memory. Finally, a Gaussian-conditioned video renderer translates scaffold renderings along user-specified camera trajectories into photorealistic video. To make this renderer practical for interaction, we train a bidirectional diffusion teacher for high-quality conditional rendering and distill it into a causal autoregressive student for bounded-latency streaming. This design combines the controllability and long-range consistency of explicit 3D representations with the perceptual quality of generative video models. MoVerse supports real-time scene roaming at 8~FPS on a single NVIDIA RTX~4090 GPU, demonstrating a practical path toward single-image world creation with interactive video output.

---

## 论文详细总结（自动生成）

好的，以下是对论文 **MoVerse: Real-Time Video World Modeling with Panoramic Gaussian Scaffold** 的详细结构化总结。

### 1. 核心问题与整体含义

-   **研究动机**：从一张窄视场角（NFOV）图像生成一个可供用户自由漫游、实时交互的完整视频世界，是计算机视觉领域一个根本性的且欠约束的挑战。单张图像仅捕获了环境的很小一部分，而交互式应用（如VR、数字孪生、具身智能）要求一个完整、几何空间一致、摄像机可控且时序连贯的 3D 世界。
-   **现状与挑战**：现有方法存在固有局限。显式 3D 重建方法具有持久的空间记忆和精确的摄像机控制，但直接从单张 NFOV 图像重建的资产往往包含空洞、浮游物和感知质量低等问题。隐式世界模型能生成高质量视频，但缺乏持久的几何记忆，导致长轨迹下场景漂移或出现不连续。混合系统试图结合两者优势，但锚点（几何引导）的密度与渲染器的实用性之间难以平衡，稀疏锚点引导力弱，而强生成式渲染器又因双向性计算成本过高，无法满足实时交互。

### 2. 方法论

-   **核心思想**：MoVerse 的核心是将“世界构建”与“观察渲染”**分离**。离线阶段，从单张图像构建一个稠密、可直接渲染的 3D 高斯支架，提供持久的空间记忆；在线阶段，由一个轻量级的因果视频渲染器将支架渲染流转化为高质量的连续视频。
-   **三阶段流水线技术细节**：
    -   **阶段一：几何感知的全景图生成** (`I → P`)
        -   **目标**：填充缺失的 360° 视场，为后续 3D 推理提供完整的观测。
        -   **关键设计**：
            1.  **重力对齐与规范观察空间**：引入一个由可微分自动调平模块和刚性变换瓶颈组成的模块，将任意姿态的输入图像校正到重力对齐的规范全景画布中，确保竖直线条垂直、地平线水平。
            2.  **拓扑感知全景补全**：采用基于掩码的潜在扩散模型进行补全。为解决等距矩形投影(ERP)全景图的水平 `S1` 周期性问题，使用了**循环潜在编码**和**平移等变扩散生成**，通过横向循环移位和一致性损失，确保生成的全景图在左右边界无缝闭合。
            3.  **规范训练数据**：构建了`Horizon360`数据集，通过将原始全景图旋转校正，强制地平线与画布赤道对齐，为模型提供了结构一致的训练目标。
    -   **阶段二：全景高斯支架构建** (`P → G`)
        -   **目标**：将完整全景图提升为一个持久的、可实时渲染的 3D 高斯泼溅（3DGS）场景资产。
        -   **关键设计**：
            1.  **ERP 感知初始化**：利用全景图深度图进行球形反投影，初始化高斯球体的中心。其初始尺度根据纬度进行校正（`s_k ∝ D_k cos ϕ_k`），以补偿 ERP 投影造成的极地区域变形。
            2.  **深度引导的残差预测**：采用前馈网络预测高斯参数的残差。关键创新在于，位置偏移 `(Δθ, Δϕ, Δz)` 是在**角度-逆深度空间**中进行预测和组合的，这保证了更新在几何上符合 ERP 的球面拓扑，并保持水平闭合。
    -   **阶段三：高斯条件化的流式视频渲染** (`\hat{V}_{1:T} → V_{1:T}`)
        -   **目标**：将支架渲染出的、带有瑕疵的 RGB 流实时转化为高质量的最终视频流。
        -   **关键设计**：
            1.  **双向条件教师模型**：基于预训练的文本到视频扩散模型（Wan2.1-T2V）进行微调，以高斯渲染流作为密集条件。通过共享目标帧与条件帧的 RoPE（旋转位置嵌入），使模型将它们对齐到同一时空坐标系。
            2.  **因果自回归学生模型**：为满足实时交互的低延迟需求，将双向教师模型蒸馏为一个因果学生模型。采用“自强迫”和分布匹配蒸馏策略，使学生模型能够以前一时刻的自回归生成结果作为缓存进行解码，生成当前帧，从而实现有界延迟的流式传输。
            3.  **渐进式训练数据混合**：设计了从简单到复杂的三阶段训练课程：(1) 单视图点云重投影视频（学习基本遮挡修复）；(2) 稀疏关键帧重建的高斯支架渲染视频（学习修复高斯泼溅伪影）；(3) 全景图构建的高斯支架渲染视频（最接近部署时的接口）。此课程逐步教会模型修复特定类型的瑕疵。

### 3. 实验设计

-   **数据集与场景**：
    -   **阶段一**：在自建的 `Horizon360` 数据集（室内外重力校正全景图）上训练。
    -   **阶段二**：使用 `HM3D` 数据集（Habitat-Matterport 3D）进行训练，利用其提供的场景与深度信息。
    -   **阶段三**：构建了一个渐进式训练数据混合体，包括：来自真视频的单视图点云重投影；稀疏关键帧重建的高斯渲染；以及在 `LayerPano3D`、`Matterport3D` 和 `Polyhaven HDRIs` 等全景数据源上，用阶段一/二流程生成的配对数据。
-   **定性评估**：论文展示了丰富的**定性结果**，包括：
    -   全流程结果：展示从输入图像到最终交互视频的完整过程。
    -   阶段一结果：展示了补全的全景图及从中采样的、跨接缝的新视角图像。
    -   阶段二结果：展示了直接从 3D 高斯支架渲染的新视角画面。
    -   阶段三结果：展示了最终因果学生模型沿用户指定轨迹生成的视频帧。
    -   教师模型结果：展示了双向教师渲染器的高质量输出，作为学生模型蒸馏的上限目标。

### 4. 资源与算力

-   **部署算力**：论文明确指出，在部署时，实现端到端 8 FPS 的场景漫游仅需**单个 NVIDIA RTX 4090 GPU**。
-   **训练细节**：论文未明确说明各阶段训练的 GPU 型号、卡数和总时长。这是论文在资源消耗透明度上的一个不足之处。

### 5. 实验数量与充分性

-   **实验组数**：论文主要通过**大量定性结果**（图 6 至图 10）来支撑其主张，覆盖了从最终效果到各中间阶段的完整流程。
-   **充分性评估**：
    -   **优点**：定性实验设计清晰，层层递进地展示了每个模块的功能和效果，特别是对全景接缝处理、跨视角渲染和骨架到视频的优化过程进行了直观呈现。
    -   **严重不足**：论文**完全缺失定量实验**。没有将 MoVerse 与任何基线方法（如 WonderJourney、Gen3C 等）在渲染质量、时序一致性、几何精度等标准指标（如 PSNR、SSIM、LPIPS、FVD）上进行数值比较。此外，也未提供针对各阶段关键模块（如拓扑感知生成、ERP 感知初始化、残差预测）的消融实验。这使得无法客观衡量其方法相较于现有工作的具体优势，也难以评估各子模块的实际贡献度，实验的严谨性和充分性存在明显缺陷。

### 6. 主要结论与发现

-   **核心发现**：证明了通过将持久空间记忆（显式 3D 高斯支架）与高保真局部生成（蒸馏后的因果视频渲染器）相分离，可以在单张图像条件下实现实时、可控、时序连贯的视频世界建模。
-   **可行性验证**：MoVerse 系统成功展示了一条从单张 NFOV 图像创建可 8 FPS 实时漫游的交互式世界的可行技术路径，验证了该混合架构的潜力。

### 7. 优点

-   **设计思想明确**：“构建”与“渲染”分离的核心思路清晰、合理，兼顾了长程一致性和实时交互性。
-   **技术创新点突出**：
    -   **全景生成**阶段对 ERP 拓扑一致性（`S1`周期性）和重力对齐的专门处理，提升了全景图的几何可信度。
    -   **高斯构建**阶段在“角度-逆深度”空间进行几何更新，是适配全景图球面几何特性的精巧设计。
    -   **视频渲染**阶段通过双向到因果的蒸馏，巧妙解决了高质量条件渲染与实时流式传输之间的矛盾。
-   **模块化设计**：三阶段流水线结构清晰，各模块可独立升级和优化，具有良好的系统扩展性。

### 8. 不足与局限

-   **完全缺乏定量评估**：
    -   未与任何现有方法进行定量对比，论文主张缺乏客观数据支持。
    -   未进行消融实验，无法评估各核心组件（如平移等变性、残差预测、训练数据混合策略）的独立贡献。
-   **误差累积与静态假设**：
    -   整个流水线是单向的，前一阶段的错误会直接传递并误导后续阶段。
    -   系统假设场景是静态的，未能处理动态物体。
-   **几何表示的固有局限**：3D 高斯支架在处理深度歧义（如透明、镜面物体）、薄结构、高空旷区域时仍会产生浮游物和空洞，视频渲染器可能仅是对这些错误进行“美化”而非修正。
-   **训练资源不透明**：论文未提及训练所需的计算资源，降低了可复现性评估的完整性。
-   **可编辑性不足**：当前模型仅支持摄像机运动，未探索对虚拟世界中的物体进行操控或编辑的能力。

（完）
