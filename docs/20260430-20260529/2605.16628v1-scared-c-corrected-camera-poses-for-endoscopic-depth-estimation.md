---
title: "SCARED-C: Corrected Camera Poses for Endoscopic Depth Estimation"
title_zh: SCARED-C：经校正的相机位姿用于内窥镜深度估计
authors: "John J. Han, Adam Schmidt, Max Allan, Jie Ying Wu, Omid Mohareri"
date: 2026-05-15
pdf: "https://arxiv.org/pdf/2605.16628v1"
tags: ["query:endo-dgs"]
score: 8.0
evidence: 纠正SCARED数据集中的相机位姿以改善内窥镜深度估计
tldr: "SCARED数据集因非关键帧依赖机器人运动学存在显著位姿误差，仅有35个关键帧可靠标注。本文提出SCARED-C，利用COLMAP重估全部帧相机位姿，并以关键帧深度图恢复尺度，将可靠RGB-D对扩展至17,135帧。通过立体视差与单目深度估计验证修正精度，并公开发布数据集与代码，大幅提升内窥镜深度估计基准的可用性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.16628v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1372, \"height\": 1706, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.16628v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 669, \"height\": 235, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.16628v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 970, \"height\": 181, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.16628v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 706, \"height\": 182, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.16628v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1484, \"height\": 1275, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.16628v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 586, \"height\": 757, \"label\": \"Table\"}]"
motivation: 原SCARED数据集仅35个关键帧可靠，非关键帧深度图受机器人运动学位姿误差影响，限制其训练价值。
method: 采用COLMAP重估所有帧相机位姿，再基于关键帧深度图恢复尺度以对齐度量空间。
result: "可靠RGB-D对从35扩展到17,135，通过立体视差评估和单目深度估计实验验证了修正位姿的有效性。"
conclusion: 发布的SCARED-C数据集显著扩大了可靠标注规模，为内窥镜深度估计提供更丰富的基准资源。
---

## 摘要
SCARED 数据集是内窥镜深度估计的一个广泛使用的基准，它提供了通过结构光传感器采集的真实三维重建。然而，非关键帧图像的深度图依赖于机器人运动学，这引入了显著的位姿误差，将数据集中可靠标注的部分限制为 35 个关键帧。我们提出了 SCARED-C，这是 SCARED 数据集的校正版本，将可靠的 RGB-D 对数量从 35 个扩展到 17,135 个。我们的流程应用了运动恢复结构系统 COLMAP 来重新估计所有帧的相机位姿，随后进行尺度恢复步骤，利用真实关键帧深度图将生成的重建对齐到度量空间。我们通过 (1) 立体视差评估和 (2) 单目深度估计实验验证了校正后的位姿。经校正的数据集和代码已公开发布给社区。

## Abstract
The SCARED dataset is a widely used benchmark for endoscopic depth estimation, offering ground-truth 3D reconstructions captured with a structured light sensor. However, the depth maps for non-keyframe images rely on robot kinematics that introduce substantial pose errors, limiting the reliably labeled portion of the dataset to 35 keyframes. We present SCARED-C, a corrected version of the SCARED dataset that expands the number of reliable RGB-D pairs from 35 to 17,135. Our pipeline applies COLMAP, a Structure-from-Motion system, to re-estimate camera poses for all frames, followed by a scale recovery step that aligns the resulting reconstructions to metric space using the ground-truth keyframe depth maps. We validate the corrected poses through (1) stereo disparity evaluation and (2) monocular depth estimation experiments. The corrected dataset and code are publicly released to the community.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：深度估计是手术计算机视觉的基础任务，可用于增强现实叠加、自主手术辅助等。然而在临床内窥镜环境中采集真实深度数据困难，现有数据集多依赖模拟、体模对齐或生成模型。
- **核心问题**：SCARED 数据集是一个基于真实离体猪腹组织的内窥镜深度估计基准，虽然提供了关键帧的优质深度真值（通过结构光传感器获取），但扩展的非关键帧使用机器人正向运动学投影产生深度图，由于达芬奇手术系统的缆线驱动引起的运动学误差，这些深度图与对应 RGB 图像严重错位，导致只有 35 个关键帧可用，其余数据不可靠。
- **整体含义**：SCARED-C 旨在纠正非关键帧的相机位姿，从而将可靠的 RGB-D 对大幅扩展，提升该数据集的训练价值。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：不使用不可靠的机器人运动学，而是直接从图像数据利用现成的运动恢复结构（SfM）管道 COLMAP 估计所有帧的相机位姿，再通过关键帧的度量深度图恢复尺度，最终将关键帧深度图重投影到所有共视帧，生成大量可靠的 RGB-D 对。
- **关键技术细节**：
  - **相机位姿估计**：
    - 对 SCARED 每个视频序列的左相机图像运行 COLMAP（原始分辨率，无额外畸变校正，因为达芬奇系统图像本身已去畸变）。
    - 使用提供的内参初始化，并在捆集调整中允许优化。
    - 输出包括优化的内参、外参和稀疏点云。
  - **尺度恢复**：
    - COLMAP 的轨迹和点云具有任意尺度（单目 SfM 的歧义性）。
    - 利用关键帧的度量深度图恢复尺度。
    - 算法步骤：
      1. 将关键帧 RGB 图像加入 COLMAP 输入，使其与序列帧在同一坐标系。
      2. 令 ˆTkf 为 COLMAP 估计的关键帧相机到世界位姿，D 为结构光传感器的度量深度图。
      3. 将 COLMAP 稀疏点云投影到关键帧像平面，得到无尺度深度图 ˆD。
      4. 计算尺度因子 s = median(D / ˆD)。
      5. 对于任意帧 i，其度量位姿为 Ti = (Ri, s · ˆti)，其中 Ri 为旋转（尺度不变），ˆti 为 COLMAP 坐标系下的相机中心。
    - 通过恢复的度量位姿将关键帧深度图重投影到各帧，生成 RGB-D 对。
    - 该过程对每个视频序列独立进行。
  - **排除条件**：仅保留 COLMAP 成功与关键帧共注册的帧；数据集 4 和 5 因本身标定差被完全排除。

## 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

- **数据集**：SCARED 数据集中的 25 个关键帧（来自数据集 1,2,3,6,7，排除数据集 4 和 5）和对应的视频序列。校正后得到 17,135 个可靠 RGB-D 对（训练 12,330，验证 4,805）。
- **评测实验**：
  - **立体视差评估**：
    - 使用现成的立体匹配模型 FoundationStereo（以及 Fast-FoundationStereo）对三种版本的深度图进行对比：原始运动学位姿投影的深度图、SCARED-C 校正后的深度图、关键帧真实深度图。
    - 将预测视差通过立体标定转为深度，计算视差终点误差（EPE）、深度绝对相对误差（Abs. Rel.）和 δ1 准确率。
    - 理由是：如果校正位姿准确，立体模型的预测应与重投影的深度图更一致。
  - **单目深度估计训练**：
    - 训练一个轻量 U-Net 单目深度估计模型（相对深度），训练数据分别为原始数据集和校正后数据集，以 25 个关键帧作为独立测试集。
    - 评估 Abs. Rel.、RMSE 和 δ1。
- **对比方法**：原始 SCARED vs. SCARED-C，并与关键帧真值作为参考上限。

## 4. 资源与算力

- 论文在立体模型评估时提及使用了 **NVIDIA H200** GPU，批次大小 1，对图像原始分辨率 1024×1280 处理，并给出了 FPS（Fast-FoundationStereo: 1.852 FPS, FoundationStereo: 0.226 FPS）。
- 对于 COLMAP 处理和单目深度估计训练的算力需求未明确说明 GPU 型号、数量或训练时长。

## 5. 实验数量与充分性：大概做了多少组实验，这些实验是否充分、客观、公平

- **实验数量**：
  - 1 组立体视差评估（对比三种数据配置，并附有逐关键帧详细结果表）。
  - 1 组单目深度估计训练对比（原始 vs. 校正，各训练一个模型）。
  - 额外包含 Fast-FoundationStereo 与 FoundationStereo 的性能对比作为参考。
- **充分性与公平性**：
  - 采用间接验证方式（无可直接评估非关键帧位姿的真值），但设计合理：利用独立的立体匹配模型作为几何一致性的检验工具，并用未见的关键帧测试单目模型训练效果，避免数据泄露。
  - 对比公平，相同架构和训练流程仅改变训练集标签。
  - 实验规模相对较小（单模型训练、无多方法消融），但足以说明校正的有效性。
  - 提供了详细的训练/验证切分建议，支持基准一致性。

## 6. 论文的主要结论与发现

- **可靠数据量大幅提升**：从 35 个关键帧扩展到 17,135 个 RGB-D 对，约 490 倍。
- **位姿校正显著提高深度与 RGB 对齐**：视觉样例和定量实验均表明校正后深度图对齐明显改善。
- **立体评估验证**：
  - 校正数据集将 EPE 从 6.062 降至 1.912（缩小 3.2 倍），Abs. Rel. 从 0.046 降至 0.026，δ1 从 96.3% 提升至 99.0%，接近关键帧真值水平（EPE 0.998, Abs. Rel. 0.014, δ1 99.7%）。
  - 降低了序列间质量方差。
- **单目训练效果提升**：使用校正数据训练的模型在关键帧测试上的 Abs. Rel. 降低 38%，RMSE 降低 35%，δ1 从 18.5% 升至 26.3%，证明校正数据提供了更有效的监督信号。
- **局限性承认**：共注册丢失部分帧，数据集 4 和 5 无法使用，重投影深度仍存在微小误差。

## 7. 优点：方法或实验设计上有哪些亮点

- **简单有效的流程**：利用成熟的 SfM 管道和简洁的尺度中值估计，避免了复杂的手动标定或硬件改动。
- **实际价值高**：将一个广泛使用的基准的可用于训练的数据量扩大了近 500 倍，极大增强了其深度学习训练潜力。
- **验证方式独特**：通过外部立体匹配模型和单目训练两种独立方式交叉验证位姿质量，增加了结果可信度。
- **公开性与基准化**：发布数据集、切分建议和代码，旨在促进行业标准化评测。
- **直观的图解**：正文图 1 对比原始与校正深度图对齐效果，一目了然。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **依赖 COLMAP 共注册**：部分帧由于视觉重叠不足（如数据集 2 keyframe 1 仅 11/88 帧被注册）被丢弃，无法覆盖全部原始序列，数据集 4、5 完全排除。
- **尺度恢复中的传播误差**：COLMAP 的重建误差和尺度因子中值估计的偏差会传递到所有重投影深度图。
- **缺乏绝对真值验证**：无法直接评估校正后非关键帧的位姿精度，仅通过间接方式推断。
- **模型训练实验单一**：仅使用一个小型 U-Net 进行相对深度估计，未探索更先进架构或绝对深度估计，结果仅供相对比较，不能作为最新方法基线。
- **未处理图像畸变等细微偏差**：虽然达芬奇图像已去畸变，但光心与图像中心不重合等细节可能引入残留误差。
- **应用限制**：校正需要关键帧有度量深度图，因此无法推广到无结构光传感器的临床数据。

（完）
